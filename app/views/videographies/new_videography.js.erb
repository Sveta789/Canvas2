$("<%= escape_javascript(render 'shared/add_videography') %>").appendTo("#tempObjects");
lightcase.start({
  href: '#add-videography',
  width: 450,
  // more options like width, height, etc.
  onClose : {
    qux: function() {
      $('#tempObjects').empty();
    }
  }
});
lightcase.resize();