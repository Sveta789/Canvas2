$("<%= escape_javascript(render 'shared/registration_form') %>").appendTo("header");
lightcase.start({
  href: '#signup-form',
  width: 450
  // more options like width, height, etc.
});
lightcase.resize();
